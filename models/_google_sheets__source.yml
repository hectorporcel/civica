version: 2

sources:
  - name: GOOGLE_SHEETS     # Nombre interno para dbt
    database: DB24_BRONZE_DB   # Tu base de datos real en Snowflake
    schema: GOOGLE_SHEETS     # Tu esquema real en Snowflake 
    
    quoting:
      database: false
      schema: false
      identifier: false 
    config:   
      freshness:
        warn_after: {count: 24, period: hour}
        error_after: {count: 48, period: hour} 

    tables:
      - name: GOOGLE_SHEETS # El nombre de la tabla que creaste en Snowflake 
        columns:
          - name: _row
          - name: product_id
          - name: quantity
          - name: month
          - name: _fivetran_synced
    
      - name: users
        columns:
          - name: Nombre
          - name: DNI
          - name: email
          - name: fecha_alta_sistema
    
  
          

          


